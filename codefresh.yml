version: '1.0'
steps:
  my_step:
    type: launch-composition
    composition:
      version: '2'
      services:
         web:
           image: ubuntu
           command: sh -c 'echo ${{CF_BRANCH}}'
